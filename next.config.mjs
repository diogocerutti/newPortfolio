/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/cerutti-portfolio",
  output: "export", // <=== enables static exports
  reactStrictMode: true,
};

export default nextConfig;
